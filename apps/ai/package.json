{
  "name": "@astro-zoom/ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8001",
    "build": "echo 'AI build complete'",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8001",
    "lint": "ruff check app/",
    "typecheck": "mypy app/ --ignore-missing-imports",
    "clean": "find . -type d -name __pycache__ -exec rm -rf {} + && find . -type f -name '*.pyc' -delete"
  },
  "dependencies": {},
  "devDependencies": {}
}
